# PostgreSQL 17+ running locally (no Docker)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/cometkit_dev

# Auth
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=15m

# API
PORT=3001
CORS_ORIGIN=http://localhost:3000
